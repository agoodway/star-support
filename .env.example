# Star Support Configuration
# This file shows all environment variables used by the Star Support demo

# Required: AI Provider Settings
AI_API_KEY="fw_your_fireworks_api_key_here"
AI_MODEL_NAME="accounts/fireworks/models/llama-v3p1-8b-instruct"

# Widget Security (Recommended to prevent API abuse)
STAR_SUPPORT_AUTH_KEY="to-the-stars-12345"

# Optional: Index Generation Settings (used by scripts/build-index.mjs)
INDEX_LANGUAGES="en"  # Comma-separated list of languages to index (default: en)
# Paths can be directories (ending with /) or specific files
# Examples: "src/content/docs/,src/pages/api/" or "README.md,docs/guide.mdx"
INDEX_INCLUDE_PATHS=""  # Comma-separated paths to include (empty = all)
INDEX_EXCLUDE_PATHS=""  # Comma-separated paths to exclude
SUMMARY_STRATEGY="standard"  # 'brief' (1 sentence), 'standard' (2-3), 'detailed' (3-4)
URL_STRATEGY="auto"  # 'auto', 'no-locale', 'locale-all'
BASE_URL=""  # Base URL for external usage (e.g., https://docs.astro.build)

# Optional: CORS configuration for external sites
# Use '*' to allow all origins, or specify a specific origin
# Examples: 'https://myapp.com' or 'https://app.example.com'
STAR_SUPPORT_CORS_ORIGIN="*"

# Optional: Maximum number of search results to use for answering questions (default: 4)
# Reduce this number to improve response speed or increase for more comprehensive answers
MAX_SEARCH_RESULTS="4"

# Required: Base path for content files at runtime
# This should match where your indexed content is stored
# For this demo: src/content/docs/en
# Note: Use INDEX_INCLUDE_PATHS in build-index to index multiple locations
CONTENT_PATH="src/content/docs/en"