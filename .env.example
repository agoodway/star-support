# Star-Support Configuration

# Required: AI Provider Settings
AI_API_KEY="fw_your_fireworks_api_key_here"
AI_API_BASE="https://api.fireworks.ai/inference/v1"
AI_MODEL_NAME="accounts/fireworks/models/llama-v3p1-8b-instruct"

# Widget Security
STAR_SUPPORT_AUTH_KEY="to-the-stars-12345"

# Optional: Index Generation Settings
INDEX_LANGUAGES="en"  # Comma-separated list of languages to index
INDEX_INCLUDE_PATHS=""  # Comma-separated paths to include (empty = all)
INDEX_EXCLUDE_PATHS=""  # Comma-separated paths to exclude
# Summary length: 'brief' (1 sentence), 'standard' (2-3 sentences), 'detailed' (3-4 sentences)
SUMMARY_STRATEGY="standard"

# Optional: URL Generation Strategy
# - 'auto': Detect based on file structure (default)
# - 'no-locale': Never include language prefix (/guides/routing/)
# - 'locale-all': Always include language prefix (/en/guides/routing/)
URL_STRATEGY="auto"

# Optional: Base URL for external usage (e.g., https://docs.astro.build)
BASE_URL=""

# Site URL for deployment (used for canonical URLs, sitemap, etc.)
# For Vercel: Set this in environment variables
# For local development: Leave empty to use default
SITE_URL=""

# Optional: CORS configuration for external sites
# Use '*' to allow all origins, or specify a specific origin
# Examples: 'https://myapp.com' or 'https://app.example.com'
STAR_SUPPORT_CORS_ORIGIN="*"